# Required
JWT_SECRET=change-this-to-a-long-random-string

# Optional — defaults shown
PORT=3001

# Set this to a path on a persistent volume so the database survives redeploys.
# Example for Railway with a volume mounted at /data:
# DB_PATH=/data/bigboards.db
# DB_PATH=
